server:
  port: 9190

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/bugtracker_db
    username: postgres
    password: Colloccollo123#
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/minibugtracker

# Freigabe fuer das Angular-Frontend (CORS).
# Mehrere Origins koennen mit Komma getrennt werden.
app:
  cors:
    allowed-origins: http://localhost:4300
